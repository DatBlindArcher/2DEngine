import game.engine.*;

public class TestScene extends Scene
{
	public void activate()
	{
		super.activate();
		GameObject.Create(new GameObject("Main Camera", new Component[] { new Camera() }), new Vector2(), 0f);
		GameObject player = new GameObject("Square", new Component[] { new Player(), new Image("Images/Square.png") });
		player.transform.scale = new Vector2(2f, 2f);
		GameObject.Create(player, new Vector2(Screen.width / 2, Screen.height / 2), 0f);
	}
}