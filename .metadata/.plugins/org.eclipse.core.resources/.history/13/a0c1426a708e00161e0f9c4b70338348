import game.engine.*;

public class TestScene extends Scene
{
	public TestScene()
	{
		//GameObject.Create(new GameObject("Main Camera", new Component[] { new Camera() }), new Vector2(Screen.width / 2, Screen.height / 2), 0f);
		GameObject.Create(new GameObject("Square", new Component[] { new Square() }), new Vector2(Screen.width / 2, Screen.height / 2), 0f);
	}
}