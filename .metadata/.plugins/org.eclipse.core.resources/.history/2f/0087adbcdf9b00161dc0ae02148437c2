package game.engine;

import java.util.Comparator;

public class AnimationFrameHandler implements Comparator<AnimationFrame>
{
	public int compare(AnimationFrame o1, AnimationFrame o2) 
	{
		if(o1.time == o2.time)
			return 0;
		if (o1.time < o2.time)
			return -1;
		return 1;
	}
}
